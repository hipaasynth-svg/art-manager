# Runtime dependencies.
#
# nooa (NVIDIA Object-Oriented Agents) is the agent runtime.
#   Source: https://github.com/NVIDIA-NeMo/labs-OO-Agents
#   It is published on PyPI, but requires Python >=3.12,<3.14.
# The pin below is intentional — nooa is pre-1.0, so minor bumps can break APIs.
nooa>=0.0.8,<0.1

pydantic>=2.0,<3.0

# Loads a project-root .env into the environment automatically (config.py).
python-dotenv>=1.0,<2.0
